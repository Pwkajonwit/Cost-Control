# แผนการปรับปรุงระบบระบุตัวตนของข้อมูล: เปลี่ยนจาก _sheetRow สู่ Primary Key (id)

แผนงานนี้จัดทำขึ้นเพื่อยกระดับสถาปัตยกรรมระบบจากการอ้างอิงตำแหน่งด้วย **เลขบรรทัด (`_sheetRow`)** ซึ่งเป็นมรดกตกทอดจาก Google Sheets/AppSheet ไปสู่การใช้ **รหัสหลักจริง (`Primary Key / id`)** บนฐานข้อมูล Supabase (PostgreSQL) ตามมาตรฐานสากล โดยเน้นหลักการ **"Zero Disruption & 100% Backward Compatibility"** (ระบบเดิมยังใช้งานได้ต่อเนื่อง ไม่พัง ไม่สะดุด)

---

## 1. วัตถุประสงค์และผลลัพธ์ที่ได้รับ
1. **หมดปัญหาข้อมูลทับแถวผิด (Row Mismatch):** การค้นหา จัดเรียง (Sort) หรือแบ่งหน้า (Pagination) จะไม่มีผลกระทบต่อการอัปเดตข้อมูลอีกต่อไป
2. **ความเร็วสูงขึ้น (Direct Index Query):** แทนที่จะโหลดข้อมูลทั้งตารางมาวนลูปค้นหา `_sheetRow` ในหน่วยความจำ ระบบจะพุ่งตรงไปอัปเดตที่ `WHERE id = ...` ในระดับฐานข้อมูลทันที
3. **ความเสถียรระยะยาว:** ลดความซ้ำซ้อนของโค้ดใน API และ Client-side Components ลงกว่า 50%

---

## 2. กลยุทธ์การเปลี่ยนผ่านอย่างปลอดภัย (Zero-Risk Strategy)

เพื่อให้การทำงานของระบบเดิมทั้งหมด (ทุกหน้าจอ ทุกตาราง) **ไม่มีปัญหาใดๆ แม้แต่จุดเดียว**:
* **Dual-Targeting (ระบบสองราง):** API จะรองรับทั้ง `id` (ตัวใหม่ที่แม่นยำ) และยังคง fallback รองรับ `sheetRow` (ตัวเดิม) ควบคู่กันไป
* **_sheetRow คงไว้เพื่อการแสดงผลเท่านั้น:** ผู้ใช้ยังคงเห็นเลขลำดับ `#1, #2, #3` บนตารางเหมือนเดิมทุกประการ เพียงแต่ระบบจะไม่นำเลขนี้ไปใช้เป็นตัวชี้เป้าในการบันทึกหรือลบลงฐานข้อมูล

---

## 3. รายละเอียดขั้นตอนการดำเนินงาน (Implementation Steps)

### ขั้นตอนที่ 1: ปรับปรุง API Layer (`app/api/rows/route.ts`)
* **PATCH & DELETE Endpoint:**
  - เพิ่มการตรวจสอบฟิลด์ `id` / `rowId` / `primaryKey` เป็นลำดับแรก
  - หากได้รับ `id` ตรง (เช่น `P026`, `CT101`, หรือตัวเลข ID ของบิล) จะทำการค้นหาและอัปเดตโดยตรงทันที ไม่ต้องวนลูปหาจาก `_sheetRow`
  - หากไม่พบ หรือฝั่ง Client รุ่นเก่ายังคงส่ง `sheetRow` มา ระบบจะยังคง fallback ตรวจสอบให้ตามปกติ

### ขั้นตอนที่ 2: ปรับปรุง Form Modal (`components/FormModal.tsx`)
* ปรับตัวแปร `editSheetRow` ให้เก็บเป็น `targetPrimaryKey` หรือ `editRowId`:
  - ดึงรหัส Primary Key ที่แท้จริงของ Entity นั้นๆ มาใช้งานเป็นอันดับแรก (เช่น `row.id`, `row["รหัสพนักงาน"]`, `row.id_store`, `row["ลำดับ"]`)
  - ในฟังก์ชัน `submitForm`: ส่ง Payload ไปยัง API เป็น `{ tableName, id: targetPrimaryKey, sheetRow: ... }` (ส่งทั้งคู่เพื่อความปลอดภัย)

### ขั้นตอนที่ 3: ปรับปรุงตารางจัดการข้อมูล (`components/ManageTableClient.tsx`)
* **ฟังก์ชันบันทึก Inline Edit (`saveEdit`):**
  - ปรับ `targetIdentifier` ให้ยึด `row.id ?? row[keyColumn]` เป็นหลัก ก่อนจะ fallback ไปหา `_sheetRow`
* **ฟังก์ชันลบข้อมูล (`confirmDelete` & `deleteSingleRow`):**
  - ใช้ `row.id ?? row[keyColumn]` ในการระบุแถวที่ต้องการลบ เพื่อไม่ให้เกิดการลบผิดแถวเวลาที่มีการค้นหาหรือจัดเรียง

### ขั้นตอนที่ 4: เสริมความปลอดภัยใน Database Layer (`lib/supabase-db.ts`)
* ในฟังก์ชัน `updateRowInSupabase` และ `deleteRowFromSupabase`:
  - ปรับปรุงการเทียบ `primaryVal` ให้ยึด Primary Key ชัดเจน และทำความสะอาดข้อมูลก้อน `data` ไม่ให้คีย์ `_sheetRow` เดิมเข้ามารบกวน
